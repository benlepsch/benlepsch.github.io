/*
    function onclick -> defined for each div that makes up the grid

    onclick() {
        if (flagged) return 0
        if (is bomb) return 'you lose'
        if (is not bomb) return getNeighbors(box.x, box.y)
    }
*/

function generateGrid() {

}

function makeBox() {
    let box = document.createElement('div');
}